{{#if @site.tracking_script}}
    <script
        defer
        data-domain="{{@site.tracking_domain}}"
        src="{{@site.tracking_script}}"
    ></script>
    <script>window.plausible = window.plausible || function() {
        (window.plausible.q = window.plausible.q || []).push(arguments) }</script>
{{/if}}